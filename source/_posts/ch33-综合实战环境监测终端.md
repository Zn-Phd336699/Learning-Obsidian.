---
title: 第33章 综合实战：环境监测终端（LVGL + MQTT + 低功耗）
date: 2025-01-01
categories:
  - 单片机开发
tags:
  - domain/mcu
  - topic/wifi
difficulty: 4
est_minutes: 45
chapter: 33
---

# 第33章 综合实战：环境监测终端（LVGL + MQTT + 低功耗）

<div style="border-left: 4px solid #0284c7; background: #f0f9ff; padding: 12px 16px; margin: 16px 0; border-radius: 0 6px 6px 0;">
<p style="margin: 0 0 8px 0; font-weight: 600; color: #0284c7;">ℹ️ 导航</p>
<div>

⏱ 45min | ★★★★☆ | 前置 [ch32-ESP32外设与WiFi-BLE上手](/posts/ch32-ESP32外设与WiFi-BLE上手/) | → [ch34-ARM-A架构与国产SoC选型地图](/posts/ch34-ARM-A架构与国产SoC选型地图/)

</div>
</div>

## 🎯 学习目标
- [ ] 完成需求拆解，产出任务划分/优先级/栈分配表与 2 倍裕量时序预算
- [ ] 按五步法移植 LVGL v9 到 SPI/RGB 屏并接入实时曲线页
- [ ] 打通 MQTT QoS1 上报、下行命令幂等去重与断网补传，通过 72h 老化交付测试

## 33.1 需求与指标

把本篇知识拧成一个可演示的产品原型：双平台实现（F407 有屏版 / ESP32-S3 无线版）。

| 需求项 | 指标 | 技术选型 |
|--------|------|----------|
| 温湿度采集 | ±0.5°C / ±3%RH，1Hz | SHT31（I2C，CRC 校验） |
| 空气质量 | PM2.5 µg/m³，10s 更新 | PMS5003（UART，帧协议） |
| 本地显示 | 实时数值 + 24h 曲线 | LVGL v9 + SPI LCD(ST7789) / RGB(LTDC) |
| 云端上报 | MQTT QoS1，30s 一次 | esp-mqtt / paho-mbedtls |
| OTA | 全量升级可回滚 | 预留 [ch30-Bootloader-IAP-OTA固件升级体系](/posts/ch30-Bootloader-IAP-OTA固件升级体系/)/[ch89-P3-MCUboot双分区OTA安全升级系统](/posts/ch89-P3-MCUboot双分区OTA安全升级系统/) 接口 |

## 33.2 BOM 物料清单（六件核心板卡/器件，双平台通用）

| 物料 | 型号/规格 | 数量 | 参考价 |
|------|-----------|------|--------|
| 主控板 | 探索者 F407 / ESP32-S3 板 | 1 | - |
| 温湿度 | SHT31-B（带 CRC，I2C） | 1 | ¥12 |
| 颗粒物 | PMS5003（UART） | 1 | ¥45 |
| 光照 | BH1750（I2C） | 1 | ¥5 |
| 显示屏 | 2.4" SPI ST7789 或 RGB 屏 | 1 | ¥40~90 |
| 蜂鸣器+杂项 | 无源蜂鸣器+S8050+排针杜邦+3D 打印外壳 | 1 套 | ¥32 |

## 33.3 任务划分与优先级（RTOS 版）

| 任务 | 周期/触发 | 优先级 | 栈(B) | 说明 |
|------|-----------|--------|-------|------|
| sensor_task | 1s 定时 | 4 | 512 | 读 SHT31/PMS，滤波入环形缓冲 |
| gui_task | LVGL 30Hz tick | 5 | 2048 | LvglHandler + 曲线刷新 |
| net_task | 队列驱动 | 6 | 4096 | MQTT 收发 + JSON 组包 |
| alert_task | 事件驱动 | 7 | 256 | 阈值判断与声光输出（状态机见 [ch07-嵌入式数据结构与算法](/posts/ch07-嵌入式数据结构与算法/)） |
| log_task | 持续 | 2 | 512 | [ch14-日志系统设计RTT与远程回传](/posts/ch14-日志系统设计RTT与远程回传/) 异步日志搬运工 |

```c
/* 数据流：全部经队列/通知传递，无共享全局变量竞态面 */
sensor_task --xQueueSend(data_evt)--> net_task      /* 上报 */
           --xTaskNotify(chart_hdl, val)--> gui_task /* 刷新 */
/* 时序预算：采集 <15ms（SHT31 单次测量 8.5ms）；MQTT 发布 <300ms；
   LVGL 单帧 <25ms（SPI@40MHz 320x240）—— 三项全部留有 2 倍裕量 */
```

## 33.4 LVGL 移植五步

1. **lv_conf.h**：使能 `LV_COLOR_DEPTH 16`、字体子集，mem_size 用静态数组；
2. **port_disp**：flush_cb 里 DMA 推送行缓冲 → `lv_disp_flush_ready()`；
3. **port_indev**：触摸 IC（XPT2046/FT6336）读取封装进 read_cb；
4. **tick 与心跳**：SysTick 中断里 `lv_tick_inc(1)`；gui_task 循环 `lv_timer_handler()` + `vTaskDelay(5)`；
5. **性能三开关**：直接模式/双缓冲取舍、DMA2D 加速取舍、`LV_USE_PERF_MONITOR` 看 FPS/CPU——验收锚点：屏幕出内容（哪怕乱码）→ FPS≥30 @ 目标分辨率 → 业务数据变化 UI 自动刷新。

## 33.5 MQTT 业务层 JSON 协议

```json
// 上行 topic: dev/{sn}/telemetry
{"t":1724470000,"temp":26.4,"rh":58.2,"pm25":35,"rssi":-62}
// 下行 topic: dev/{sn}/cmd （QoS1 + retained=false）
{"id":42,"act":"set_threshold","pm25":75}
```

设计要点：cmd 带 `id` 幂等去重；断网期间本地缓存最近 100 条补传（带 seq）且队列必须设上限防撑爆。云协议本体深究见 [ch80a-MQTT-CoAP云协议本体与实现](/posts/ch80a-MQTT-CoAP云协议本体与实现/)。

## 33.6 参数调试技巧

| 症状 | 测量手段 | 调什么 | 判据 |
|------|----------|--------|------|
| 休眠功耗与计算偏差大 | 电流表实测均值 vs 理论值 | 背光策略/休眠占空比 | 偏差 <20%（交付指标） |
| FPS 不达标 | LV_USE_PERF_MONITOR | SPI 时钟拉满/DMA 双缓冲/降非关键刷新率 | FPS≥30 @ 目标分辨率 |
| 老化后 heap 缩水 | heap_caps 按 malloc 调用点聚类存活数 | cJSON_Delete 配对释放 | 72h 内存水位零增长 |

## 33.7 实测数据表（时序预算）

| 场景 | 预算上限 | 依据 |
|------|----------|------|
| 1Hz 传感器采集 | <15ms | SHT31 单次测量 8.5ms |
| MQTT 发布序列 | <300ms | 含组包与发布全链路 |
| LVGL 单帧渲染 | <25ms | SPI@40MHz 320x240，留 2 倍裕量 |

## 33.8 开源项目对照与差距分析

| 对照对象 | 亮点可借鉴 | 我们的差距点 |
|----------|------------|--------------|
| 立创开源广场「环境监测站」类项目（关键词：环境监测 STM32） | 外壳结构/PCB 天线布局/物料 BOM 成熟度 | 工业设计与 EMC 细节 |
| GitHub: ESPHome | YAML 配置驱动的传感器抽象层设计 | 设备抽象统一接口（[ch08-分层架构与设计模式](/posts/ch08-分层架构与设计模式/) 思想的极致版） |
| GitHub: WLED | Web 配置界面 + mDNS 发现的零配置体验 | 配网体验：SmartConfig/AP 配置门户二选一落地 |

## 33.9 排故速查表

| 现象 | 根因候选 | 定位路径 |
|------|----------|----------|
| 曲线刷新整机卡顿 | 曲线每点重绘整图/SPI 未走 DMA | lv_chart 只推新点(set_next_value)+曲线区降 2Hz；确认 DMA 且 flush_cb 不忙等 |
| broker 连上但发布丢失 | QoS0 不保证送达 | 改 QoS1+seq 去重；断网缓存队列设上限防撑爆 |
| SHT31 偶发读数全 F | I2C 总线干扰或上拉不足 | 示波器看边沿([ch18-示波器实战](/posts/ch18-示波器实战/))；CRC 失败重试+连续失败告警；总线卡死走 [ch76-I2C协议与排障时钟拉伸总线锁死多主机](/posts/ch76-I2C协议与排障时钟拉伸总线锁死多主机/) 解锁流程 |

## 33.10 部署与交付：72h 老化清单（交付前必过五关）

1. **72 小时老化**：内存水位无增长、无重启、上报成功率 ≥99.5%；
2. **断网恢复**：拔网线 10 分钟插回，缓存补传且不重复；
3. **异常注入**：SHT31 拔除→UI 显示"--"且告警不崩溃；PM 乱码帧→解析器自愈；
4. **功耗实测**（电池版）：休眠均值 vs 计算值偏差 <20%；
5. **OTA 演练**：升级中断电→回滚成功→再次升级成功。

> [!example]- 🧪 动手实验 L33-1：故障注入生存测试（30 分钟）
> **步骤**：运行完整固件 → 运行中拔掉 SHT31 观察界面与告警 → 重新插入观察恢复 → 再向 PMS 串口灌入乱码字节流。
> **验收**：拔除期间 UI 显示"--"、声光告警、系统无重启；乱码帧被丢弃且解析器自愈；重新接入后 3s 内恢复有效读数并继续上报。

## 33.11 进阶话题

- **双平台抽象收益兑现**：App 层零改动同时跑在 F407 与 ESP32-S3——检验 [ch08-分层架构与设计模式](/posts/ch08-分层架构与设计模式/) 成色，简历硬核亮点；
- **从终端到系统的演进**：加 RS485 变网关（[ch44-综合实战RK3568多协议边缘网关](/posts/ch44-综合实战RK3568多协议边缘网关/)）→ RTSP 监控节点（[ch66-综合实战USB摄像头流采集服务](/posts/ch66-综合实战USB摄像头流采集服务/)）；LoRa 组网方向指向 [ch83-LoRaWAN组网LoRaMac-node-ChirpStack](/posts/ch83-LoRaWAN组网LoRaMac-node-ChirpStack/)/[ch90-P4-LoRa温湿度采集网关ChirpStack后端](/posts/ch90-P4-LoRa温湿度采集网关ChirpStack后端/)；
- **展示技巧**：演示视频必含「故障注入恢复」片段——评委/面试官对稳定性的兴趣远大于功能演示。

> [!warning]- ❓ FAQ
> **Q1：LVGL 和业务逻辑抢 CPU 卡顿？** 渲染放最高可用优先级但每帧主动让步；SPI 时钟拉满并用 DMA 行推送；降低非关键刷新率（曲线区 5Hz 足够）。终极方案是 F407 用 LTDC+双层显存直接模式。
> **Q2：PM2.5 改成 I2C 型号要改多少？分层是否合格如何验证？** 仅 sensor_task 的传感器适配层要改，gui/net/alert 零改动——这就是检验分层的试金石。

<div style="border-left: 4px solid #d97706; background: #fffbeb; padding: 12px 16px; margin: 16px 0; border-radius: 0 6px 6px 0;">
<p style="margin: 0 0 8px 0; font-weight: 600; color: #d97706;">❓ 📝 思考题</p>
<div>

1. 设计断电续传的 seq 方案并推演各种掉电时序下的正确性（提示：seq 先持久化再入缓存，云端按 sn+seq 幂等去重）。
2. 给出本项目向「多房间组网」演进的两种拓扑（星型 BLE Mesh vs LoRa 星型）对比。

</div>
</div>

---
🏷️ #domain/mcu #topic/wifi | 🔗 [ch32-ESP32外设与WiFi-BLE上手](/posts/ch32-ESP32外设与WiFi-BLE上手/) ← **本章** → [ch34-ARM-A架构与国产SoC选型地图](/posts/ch34-ARM-A架构与国产SoC选型地图/) | 📚 [P3-MOC](/posts/P3-MOC/)
